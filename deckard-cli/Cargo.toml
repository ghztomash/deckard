[package]
name = "deckard-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive", "cargo"] }
colored = "2"
env_logger = "0.11"
deckard = {path = "../deckard"}
